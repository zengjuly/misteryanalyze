正在读取最新相关代码，给出可直接落地的修改方案。# Hermes 执行规范：财务 ths 优先 + 行业 session 缓存 + 综合分析传入行业

基于当前 main 仓库，只改下列文件，禁止扩 scope、禁止打印全量数据。

| 文件 | 改动 |
|------|------|
| data/financial_storage.py | ensure_financial：ths valuations-snapshot 优先，再 baostock |
| web/pages/1_📈_个股分析.py | industry_map session；算 industry_trend 并传入 comprehensive_signal_analysis |
| （可选加固）data/ths_client.py | fetch_financials 参数与 CLI 一致 --thscodes |

A. data/financial_storage.py — 整段替换 ensure_financial

缓存有效条件改为：有 PE 或 PB 或 报告期/ROE/EPS（ths 只给估值时也能命中缓存）。

    def ensure_financial(self, code: str, client=None) -> Dict:
        """确保财务可用：优先 ThsOfficialClient.valuations-snapshot，再 baostock。
        :param code: sh.600150 或 sh600150
        :return: {报告期/ROE/EPS/PE/PB/股息率/...}，失败 {}
        """
        fi = self.load_latest(code)
有 PE/PB 或报告期/ROE/EPS 即视为有效（ths 快照无报告期）
        if fi and (
            fi.get('PE') is not None or fi.get('PB') is not None
            or fi.get('报告期') or fi.get('ROE') is not None
            or fi.get('EPS') is not None
        ):
            return fi

        code_dot = code if '.' in code else (
            code[:2] + '.' + code[2:] if len(code) > 2 else code)
        code_nodot = code.replace('.', '')

----- 1. 优先同花顺扶摇 valuations-snapshot -----
        try:
            import yaml
            import os
            from datetime import date
            cfg_path = os.path.join(
                os.path.dirname(os.path.dirname(os.path.abspath(file))),
                'config', 'config.yaml')
            with open(cfg_path, encoding='utf-8') as f:
                cfg = yaml.safe_load(f) or {}
            from ths_client import ThsOfficialClient
            ths = ThsOfficialClient(cfg)
            snap = ths.fetch_financials(code_nodot)
            pe = snap.get('pe') or snap.get('pe_ttm')
            pb = snap.get('pb') or snap.get('pb_mrq')
全 0 / 全空视为失败，走兜底
            if pe or pb:
                row = {
                    '报告期': date.today().isoformat(),  # 估值快照日占位
                    'PE': pe if pe else None,
                    'PB': pb if pb else None,
                    'ROE': snap.get('roe') or None,
                    'EPS': None,
                    '股息率': snap.get('dividend_yield') or None,
                }
去掉纯 0 的 ROE/股息占位，避免污染
                if not row.get('ROE'):
                    row.pop('ROE', None)
                if not row.get('股息率'):
                    row.pop('股息率', None)
                self.save_financial(code_dot, row)
                latest = self.load_latest(code_dot)
                if latest:
                    return latest
        except Exception as e:
            logger.warning(f"⚠️ ensure_financial ths 失败({code}): {str(e)[:80]}")

----- 2. 兜底 MultiSourceClient / baostock（保留） -----
        try:
            if client is None:
                import yaml
                import os
                from multi_source_client import MultiSourceClient
                cfg_path = os.path.join(
                    os.path.dirname(os.path.dirname(os.path.abspath(file))),
                    'config', 'config.yaml')
                with open(cfg_path, encoding='utf-8') as f:
                    cfg = yaml.safe_load(f)
                client = MultiSourceClient(cfg)
            if not getattr(client, 'login_success', False):
                client.login()
            current_price = None
            try:
                kdf = self.db.load_kline(code_dot, 'daily')
                if kdf is not None and not kdf.empty:
                    current_price = float(kdf['close'].iloc[-1])
            except Exception:
                pass
            data = client.get_financial_data(code_nodot, current_price)
            if not data:
                return {}
            self.save_financial(code_dot, data)
            return self.load_latest(code_dot)
        except Exception as e:
            logger.warning(f"⚠️ ensure_financial 兜底失败({code}): {str(e)[:80]}")
            return {}

说明：

ths 官方 无 ROE/股息率 时页面 ROE/股息率可为 N/A，PE/PB 仍可用。
禁止在成功路径默认 baostock.login()。

可选：ths_client.fetch_financials 与 CLI 对齐

若实测 CLI 要 --thscodes：

    def fetch_financials(self, stock_code: str) -> dict:
        ths_code = self._to_ths_code(stock_code)
        raw = self._run_fuyao(
            ['valuations-snapshot', '--thscodes', ths_code])
若失败可再试 --thscode（兼容旧脚本）
        if not raw:
            raw = self._run_fuyao(
                ['valuations-snapshot', '--thscode', ths_code])
        ...

其余映射逻辑保持现有 pe_ttm / pb_mrq。

B. web/pages/1_📈_个股分析.py — 行业 map session + 传入行业趋势

B1. 页面顶部（logic = get_logic() 之后）增加行业 map 缓存

行业分类 session 缓存（避免每票/每次 rerun 全量读）
if 'industry_map' not in st.session_state:
    try:
        st.session_state['industry_map'] = feeder.get_industry_data()
    except Exception:
        st.session_state['industry_map'] = {}

B2. 所属板块展示：只用 session，禁止再调 get_industry_data()

替换：

            industry_name = '未知'
            try:
                ind_map = feeder.get_industry_data()
                cm = ind_map.get('code_map', {})
                industry_name = cm.get(db_code, cm.get(code, '未知'))
            except Exception:
                pass
            st.info(f"🏢 所属板块: {industry_name}")

为：

            ind_map = st.session_state.get('industry_map') or {}
            cm = ind_map.get('code_map') or {}
            multi = ind_map.get('multi_map') or {}
            blocks = multi.get(db_code) or multi.get(code)
            if blocks:
                industry_name = '、'.join(str(b) for b in blocks if b)
            else:
                industry_name = cm.get(db_code) or cm.get(code) or '未知'
            st.info(f"🏢 所属板块: {industry_name}")

B3. 综合分析前计算 industry_trend 并传入

在 未命中 analysis_cache 分支内，替换：

                signal = logic.comprehensive_signal_analysis(
                    daily, weekly_data=weekly, market_data=market_data,
                    industry_data=None, industry_trend=None)

为：

行业趋势：优先板块强度 map（session/模块缓存），否则仅 bool 缺失
                industry_trend = None
                industry_data = None
                try:
                    ind_map = st.session_state.get('industry_map') or {}
                    cm = ind_map.get('code_map') or {}
                    ind_name = cm.get(db_code) or cm.get(code)
                    if ind_name:
                        from web.pages_util import build_sector_strength_map
若 pages_util 路径不同：from pages_util import ...
                        smap = build_sector_strength_map()
session 缓存强度 map，避免每票重算
                        if 'sector_strength_map' not in st.session_state:
                            st.session_state['sector_strength_map'] = smap
                        else:
                            smap = st.session_state['sector_strength_map'] or smap
                        score = smap.get(ind_name)
                        if score is not None:
与板块监控同一套得分：>0 视为向上
                            industry_trend = bool(float(score) > 0)
                except Exception:
                    industry_trend = None

                signal = logic.comprehensive_signal_analysis(
                    daily, weekly_data=weekly, market_data=market_data,
                    industry_data=industry_data,
                    industry_trend=industry_trend)

MysteryLogic.three_resonance_analysis 已支持 industry_trend is True/False（无 industry_data 时），不必改 logic。

build_sector_strength_map 若仍走个股抽样，属既有实现；有指数强度后再换实现即可，本页只消费 map。

首次把 sector_strength_map 写入 session 后，同会话换股不再全表重算（与 pages_util 30min 缓存叠加）。

C. 验收（短输出）

财务：无缓存时日志应先 ths，不应先 baostock login
python -c "
from data.db_manager import MysteryDB
from data.financial_storage import FinancialStorage
fs=FinancialStorage(MysteryDB())
可先清该 code 财务行再测
print(fs.ensure_financial('sh.600519'))
"

页面：连续分析两只票，industry_map / sector_strength_map 仅应构建一次

检查：

ensure_financial 无缓存时走 ths，有 PE/PB 写入 stock_financial_data。  
个股页不每票调用 get_industry_data()。  
未命中 signal 缓存时，comprehensive_signal_analysis(..., industry_trend=True/False) 详情含「行业板块同步走强」或「走弱」。

D. 提交说明

perf: 财务优先ths估值快照；行业map/板块强度session缓存并传入三振

E. 禁止

不要在 ensure_financial 成功 ths 后再强制 baostock.login  
不要每次分析 feeder.get_industry_data()  
不要 industry_data=None 且 industry_trend=None 双空（有板块名时应尽力算 bool）  
不要向 Agent 打印完整 valuations / 行业全表  

按 A → B1 → B2 → B3 顺序改即可。

