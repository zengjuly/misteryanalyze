Hermes 执行规范：扩大 analysis_cache + POC 只算末窗

基于当前 main。只改 2 个文件；db_manager 的 report_json 已支持任意 dict（json.dumps(..., default=str)），表结构不用改。

| 文件 | 改动 |
|------|------|
| analysis/adaptive_platform.py | POC 支持 latest_only（展示只算末窗） |
| web/pages/1_📈_个股分析.py | 缓存载荷含 ap/plat/det/cl；命中跳过重算 |

扫描/全序列路径保持 latest_only=False（默认）。

A. analysis/adaptive_platform.py

A1. calculate_adaptive_vap_atr 增加参数 latest_only: bool = False

在函数签名末尾增加 latest_only: bool = False。

将原 POC 滚动段（约 158–178 行）：

    poc_series = []
    for i in range(len(df)):
        if i = n:
            window = df.iloc[-n:]
            poc_series[-1] = get_cn_poc(window)
        df['poc'] = poc_series
    else:
        poc_series = []
        for i in range(len(df)):
            if i < n - 1:
                poc_series.append(np.nan)
            else:
                window = df.iloc[i - n + 1: i + 1]
                poc_series.append(get_cn_poc(window))
        df['poc'] = poc_series

上下轨、突破信号逻辑不变（依赖 poc/matr；末窗模式下仅最后一行 POC 有效，展示只读 iloc[-1]，足够）。

A2. analyze_adaptive_platform 透传 latest_only

签名增加 latest_only: bool = True（个股展示默认 True）。

调用处改为：

        df = calculate_adaptive_vap_atr(
            data, n=n, atr_m=atr_m, k=k, market_type=market_type,
            latest_only=latest_only)

run_market_scan / 需要全序列 is_breakout 的调用处显式传 latest_only=False。若扫描调的是 calculate_adaptive_vap_atr 而非 analyze_adaptive_platform，默认 False 即可，不必改扫描。

全文搜索：

analyze_adaptive_platform(
calculate_adaptive_vap_atr(

扫描入口若用 analyze_adaptive_platform 且依赖历史突破列，则加 latest_only=False。

B. web/pages/1_📈_个股分析.py — 扩大缓存并跳过 ⑥⑦

B1. 缓存读写（替换约 116–135 行）

===== 分析结果缓存（signal + 明细 ap/plat/det/cl）=====
            db_code = code[:2] + '.' + code[2:] if '.' not in code else code
            cached = db.get_analysis_cache(db_code, 'daily', last_date)
            ap = plat = det = cl = None
            if cached and cached.get('signal'):
                signal = cached['signal']
                ap = cached.get('ap') or {}
                plat = cached.get('plat') or {}
                det = cached.get('det') or {}
                cl = cached.get('cl') or {}
                st.caption(
                    f"⚡ 命中分析缓存（最新交易日 {last_date}，"
                    f"含明细载荷，跳过重复计算）")
            else:
                weekly = feeder.get_weekly(code)
                if 'market_data_cache' not in st.session_state:
                    with st.spinner("加载大盘指数数据..."):
                        st.session_state['market_data_cache'] = \
                            feeder.get_market_index()
                market_data = st.session_state['market_data_cache']
                signal = logic.comprehensive_signal_analysis(
                    daily, weekly_data=weekly, market_data=market_data,
                    industry_data=None, industry_trend=None)

明细一次算完写入缓存（可 JSON 序列化）
                try:
                    from analysis.adaptive_platform import (
                        analyze_adaptive_platform)
                    ap = analyze_adaptive_platform(
                        daily, stock_code=code, latest_only=True)
                except Exception:
                    ap = {}
                try:
                    plat = logic.platform_breakthrough_analysis(daily)
                except Exception:
                    plat = {}
                try:
                    det = logic.technical_detail_capture(daily)
                except Exception:
                    det = {}
                try:
                    cl = logic.main_bull_wave_checklist(daily)
                except Exception:
                    cl = {}

                db.set_analysis_cache(db_code, 'daily', last_date, {
                    'signal': signal,
                    'ap': ap,
                    'plat': plat,
                    'det': det,
                    'cl': cl,
                })

注意：明细计算挪到 写缓存之前；命中时不再进 else。

B2. 明细展示段（约 185–214 行）— 禁止无条件重算

替换原「每次都 analyze / platform / technical / checklist」为：

---------- 3. Excel 对齐明细 ----------
            st.subheader("📋 分析明细（与Excel报告对齐）")
缓存未带齐明细时补算（兼容旧缓存仅含 signal）
            if not ap:
                try:
                    from analysis.adaptive_platform import (
                        analyze_adaptive_platform)
                    ap = analyze_adaptive_platform(
                        daily, stock_code=code, latest_only=True)
                except Exception as e:
                    ap = {}
                    st.caption(f"自适应平台: {e}")
            if not plat:
                try:
                    plat = logic.platform_breakthrough_analysis(daily)
                except Exception:
                    plat = {}
            if not det:
                try:
                    det = logic.technical_detail_capture(daily)
                except Exception:
                    det = {}
            if not cl:
                try:
                    cl = logic.main_bull_wave_checklist(daily)
                except Exception:
                    cl = {}

            p1, p2, p3 = st.columns(3)
            p1.metric("自适应平台 POC", ap.get('POC', 'N/A'))
            p2.metric("平台上轨", ap.get(
                '自适应上轨', ap.get('上轨', ap.get('upper', 'N/A'))))
            p3.metric("平台下轨", ap.get(
                '自适应下轨', ap.get('下轨', ap.get('lower', 'N/A'))))
            if plat.get('平台范围'):
                st.markdown(
                    f"震荡区间（平台箱体）: {plat.get('平台范围')} "
                    f"| 状态: {plat.get('平台状态', '未知')}")
            st.markdown(
                f"筹码分析: 集中度 {det.get('筹码集中度', '未知')} "
                f"| 趋势 {det.get('筹码趋势', '未知')}")
            if cl.get('详情'):
                items = [d for d in cl['详情'] if '✅' in d or '❌' in d]
                if items:
                    st.markdown(
                        f"主升浪8项指标对比（满足 {cl.get('满足数量', 0)}/8）")
                    for it in items:
                        st.markdown(f"- {it}")

其后周/月箱体、K 线、day_box 中 ap.get('POC') 保持；保证 ap 始终为 dict（上面已兜底 {}）。

            day_box = {
                '上沿': float(daily['最高价'].tail(20).max()),
                '下沿': float(daily['最低价'].tail(20).min()),
                'POC': (ap or {}).get('POC'),
            }

B3. 旧缓存兼容

仅含 signal、无 ap 时：展示段 if not ap 会补算一次；可选在补算后：

                db.set_analysis_cache(db_code, 'daily', last_date, {
                    'signal': signal,
                    'ap': ap, 'plat': plat, 'det': det, 'cl': cl,
                })

写回完整载荷（同一 last_date，不强制）。

C. 序列化注意

set_analysis_cache 已用 default=str，numpy/bool 可进 JSON。
不要把 DataFrame 放进 ap/plat/det/cl。
plat/cl/det/ap 保持现有返回的纯 dict/list/标量。

D. 验收

POC 末窗：长序列应明显快于全滚动（可本地计时）
python -c "
import time
import pandas as pd
from analysis.adaptive_platform import analyze_adaptive_platform
用本地任一有日K的代码经 feeder 拉 df 后：
t0=time.time(); analyze_adaptive_platform(df, latest_only=True); print('last', time.time()-t0)
t0=time.time(); analyze_adaptive_platform(df, latest_only=False); print('full', time.time()-t0)
print('ok')
"

页面：

首次分析某股：写缓存，JSON 含 signal,ap,plat,det,cl。  
同交易日再分析：caption 命中缓存，日志/耗时无二次 platform_breakthrough / 全量 POC 循环。  
扫描任务 POC 序列行为与改前一致（默认 latest_only=False）。

E. 提交说明

perf: analysis_cache 含ap/plat/det/cl；展示POC仅末窗

F. 禁止

改 mystery_analysis_cache 表结构  
展示路径默认全历史 POC 循环  
扫描默认 latest_only=True  
向终端打印完整 cache JSON  

按 A1 → A2 → B1 → B2 顺序修改即可。
