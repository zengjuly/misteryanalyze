Hermes 执行规范：个股分析页前端体验优化

唯一主文件： web/pages/1_📈_个股分析.py  
不改后端逻辑库（除非自选刷新按钮需要）；禁止扩 scope、禁止大段 print。

0. 改动清单

| 项 | 做法 |
|----|------|
| 图表懒加载 | 默认只 plot_kline 日线；周/月用 st.radio 再画 |
| 自选股 session | list_all 进 session，TTL 或按钮刷新 |
| 分阶段 spinner | 日K → 信号 → 财务 → 图 分段提示 |
| 搜索 | term 长度 &lt; 2 不扫全表；可选简易索引 |
| 最近 20 日 | 删除或放进 st.expander 默认折叠 |
| 代码无点 | 解析后统一 replace('.', '') 再查名称 |

1. 代码无点 + 名称

在 resolve_code 之后、开始分析时：

    if _jump_code:
        code = _jump_code
    elif not selected:
        st.warning("请选择或输入股票代码")
        st.stop()
    else:
        code = resolve_code(selected)
    if not code:
        st.warning("无法解析股票代码")
        st.stop()

    code = str(code).replace('.', '').strip()
    name = stock_dict.get(code, '')
    if not name:
        try:
            from db_manager import MysteryDB
            info = MysteryDB().get_stock_info(limit=None)
            if info is not None and not info.empty:
                for c, n in zip(info['code'], info['code_name']):
                    if str(c).replace('.', '') == code and n and str(n) != 'nan':
                        name = str(n)
                        break
        except Exception:
            pass
    if not name:
        name = code

db_code 仍用：code[:2] + '.' + code[2:]。

2. 搜索优化

替换 search_stock：

def search_stock(term: str):
    term = (term or '').lower().strip()
    if not term:
        return [f"{c} - {n}" for c, n in list(stock_dict.items())[:30]]
单字符不扫全表，避免每次按键全市场遍历
    if len(term)  WL_TTL
        or st.session_state.get('watchlist_force_refresh')
    )
    if need_wl:
        try:
            from watchlist_manager import WatchlistManager
            wl = WatchlistManager().list_all()
            wl_codes = wl['code'].tolist() if not wl.empty else []
            pool_names = [
                f"{c.replace('.', '')} - "
                f"{stock_dict.get(c.replace('.', ''), n)}"
                for c, n in zip(
                    wl_codes,
                    wl['name'].tolist() if not wl.empty else [])]
        except Exception:
            pool_names = [f"{s} - {stock_dict.get(s, '')}"
                          for s in cfg.get('stocks', [])]
        st.session_state['watchlist_pool_names'] = pool_names
        st.session_state['watchlist_ts'] = time.time()
        st.session_state['watchlist_force_refresh'] = False
    pool_names = st.session_state.get('watchlist_pool_names') or []

    r1, r2 = st.columns([4, 1])
    with r1:
        pool_sel = st.selectbox("⭐ 自选股", [''] + pool_names)
    with r2:
        if st.button("🔄", help="刷新自选股"):
            st.session_state['watchlist_force_refresh'] = True
            st.rerun()
    if pool_sel:
        selected = pool_sel

每次 searchbox 触发的 rerun 不再打 list_all()，除非 TTL 到期或点刷新。

4. 分阶段 spinner + 分析主路径骨架

在 with st.spinner(...) 内改为分段（可保留外层一个 spinner，或拆成多段 status）：

    if st.button("🚀 开始分析", type="primary", width="stretch") or _jump_code:
... code/name 解析见 §1 ...

        try:
            from db_manager import MysteryDB
            db = MysteryDB()
            db_code = code[:2] + '.' + code[2:]

----- 阶段1：日K -----
            with st.spinner("① 加载日K..."):
若已做「日K只拉一次」，此处写 long_df 逻辑；否则：
                daily = feeder.get_daily(code)
                if daily is None or daily.empty:
                    st.error(f"❌ 无法获取 {code} 行情数据")
                    st.stop()
                last_date = str(daily['日期'].max())[:10]

----- 阶段2：信号（含缓存）-----
            with st.spinner("② 综合信号分析..."):
                cached = db.get_analysis_cache(db_code, 'daily', last_date)
                ap = plat = det = cl = None
                if cached and cached.get('signal'):
                    signal = cached['signal']
                    ap = cached.get('ap') or {}
                    plat = cached.get('plat') or {}
                    det = cached.get('det') or {}
                    cl = cached.get('cl') or {}
                    st.caption(f"⚡ 命中分析缓存（{last_date}）")
                else:
                    weekly = feeder.get_weekly(code)
                    if 'market_data_cache' not in st.session_state:
                        st.session_state['market_data_cache'] = \
                            feeder.get_market_index()
                    market_data = st.session_state['market_data_cache']
                    signal = logic.comprehensive_signal_analysis(
                        daily, weekly_data=weekly, market_data=market_data,
                        industry_data=None, industry_trend=None)
若已实现扩大 cache，此处算 ap/plat/det/cl 并 set_analysis_cache
                    db.set_analysis_cache(db_code, 'daily', last_date,
                                          {'signal': signal,
                                           'ap': ap or {}, 'plat': plat or {},
                                           'det': det or {}, 'cl': cl or {}})

            st.success(f"✅ {code} {name} 分析完成（最新交易日 {last_date}）")
板块展示、评分卡片 … 保持

----- 阶段3：财务 -----
            st.subheader("💰 财务数据")
            with st.spinner("③ 加载财务..."):
                try:
                    from financial_storage import FinancialStorage
                    fs = FinancialStorage(db)
                    fi = fs.ensure_financial(db_code)
… 原 metric 展示逻辑不变 …
                except Exception as e:
                    st.warning(f"财务数据获取失败: {e}")

----- 阶段4：明细（若未在 cache）+ 图 -----
明细 ap/plat/det/cl 展示逻辑保持
图见 §5

要点：用多个短 with st.spinner 替代整页一个「正在分析…」，便于定位卡顿。

5. 图表懒加载（核心）

删除 st.tabs 三个 with 里一次性三张 plotly_chart。

改为：

            st.subheader("📊 K线图（默认日线；周/月按需生成）")
            from indicators.ma_indicators import MAIndicators
            from kline_resampler import KLineResampler
            rs = KLineResampler()

long_df：与分析共用；若尚未做「只拉一次」，至少 session 缓存
            kkey = f'kline_long_{code}'
            if kkey not in st.session_state:
                long_df = daily
                st.session_state[kkey] = long_df
            long_df = st.session_state[kkey]

            def _prep_kline(kdf, box_dict, max_bars=1000):
                kdf = kdf.copy()
                if len(kdf) > max_bars:
                    kdf = kdf.tail(max_bars)
                if 'MA377' not in kdf.columns:
                    kdf = MAIndicators().calculate_ma(kdf)
                if 'EMA20' not in kdf.columns:
                    kdf = MAIndicators().calculate_ema(kdf)
                return plot_kline(
                    kdf, title=f"{code} {name}", box=box_dict,
                    max_bars=max_bars)

            day_box = {
                '上沿': float(daily['最高价'].tail(20).max()),
                '下沿': float(daily['最低价'].tail(20).min()),
                'POC': (ap or {}).get('POC') if ap else None,
            }

            period_sel = st.radio(
                "周期",
                ["📅 日线", "📆 周线", "🗓️ 月线"],
                horizontal=True,
                key=f"kline_period_{code}",
            )

            with st.spinner("④ 绘制K线..."):
                if period_sel.startswith("📅"):
                    st.plotly_chart(
                        _prep_kline(long_df, day_box, max_bars=1000),
                        width="stretch")
                    st.caption(
                        f"日线: 最近 {min(len(long_df), 1000)} 个交易日")
                elif period_sel.startswith("📆"):
                    wk_long = rs.resample(long_df, 'weekly')
                    if wk_long is not None and len(wk_long):
                        w_hi = float(wk_long['最高价'].tail(20).max())
                        w_lo = float(wk_long['最低价'].tail(20).min())
                        st.plotly_chart(
                            _prep_kline(
                                wk_long, {'上沿': w_hi, '下沿': w_lo},
                                max_bars=200),
                            width="stretch")
                        st.caption(
                            f"周线: {w_lo:.2f} ~ {w_hi:.2f} | "
                            f"最新 {wk_long['收盘价'].iloc[-1]:.2f}")
                    else:
                        st.info("周线数据不足")
                else:
                    mo_long = rs.resample(long_df, 'monthly')
                    if mo_long is not None and len(mo_long):
                        m_hi = float(mo_long['最高价'].tail(12).max())
                        m_lo = float(mo_long['最低价'].tail(12).min())
                        st.plotly_chart(
                            _prep_kline(
                                mo_long, {'上沿': m_hi, '下沿': m_lo},
                                max_bars=50),
                            width="stretch")
                        st.caption(
                            f"月线: {m_lo:.2f} ~ {m_hi:.2f} | "
                            f"最新 {mo_long['收盘价'].iloc[-1]:.2f}")
                    else:
                        st.info("月线数据不足")

说明：Streamlit radio 切换会 rerun，但每次只生成 1 张图；默认日线，首次不再画周/月。

分析明细里的周/月箱体文字（rs.resample(daily) 算高低）可保留，成本远低于 Plotly。

6. 去掉或折叠「最近 20 日」

推荐：默认折叠（仍可导出）：

---------- 最近20日（默认折叠）----------
            with st.expander("🗓️ 最近20个交易日数据（点击展开）", expanded=False):
                tail = daily.tail(20).copy()
                tail['日期'] = tail['日期'].astype(str)
                show_cols = ['日期', '收盘价', '开盘价', '最高价', '最低价',
                             '成交量', '换手率']
                show_cols = [c for c in show_cols if c in tail.columns]
                st.dataframe(tail[show_cols].round(3), width="stretch")
                st.download_button(
                    "📥 导出CSV",
                    tail[show_cols].to_csv(index=False).encode('utf-8-sig'),
                    f"{code}_daily.csv", "text/csv",
                    key=f"csv20_{code}")

若产品确认不要表：删除整块；Excel 导出只保留信号摘要一行（见此前方案）。

原顶层 st.subheader("🗓️ 最近20个交易日数据") + 直接 st.dataframe 删除。

7. 验收

打开个股分析，不点分析：自选不每键 list_all（可断点/日志确认）。  
点分析：依次出现 ①日K ②信号 ③财务 ④图（或等价 spinner 文案）。  
默认仅 1 张日线图；切「周线」才第二张。  
搜索输入 1 个字符：不出现全市场 in 慢卡。  
sh.600984 / sh600984 成功行显示真实名称（有字典时）。  
最近 20 日默认不展开。

8. 提交说明

perf(web): 个股页图懒加载/自选缓存/分阶段spinner/搜索与代码规范化

9. 禁止

恢复 st.tabs 三个分支同时 plotly_chart  
每次 rerun 无条件 WatchlistManager().list_all()  
搜索 len(term)<2 时全表 term in name  
修改无关页面（板块/扫描）  

按 §1 → §2 → §3 → §4 → §5 → §6 顺序改同一文件即可。

