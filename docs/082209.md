Hermes 执行规范：个股分析展示对齐 + daily 与 Web 流程统一

基于当前 main：main.py 的 _perform_mystery_analysis 已产出完整字段；Web 页仅用了 comprehensive_signal_analysis 的精简 signal。目标：同一套 pipeline，Web 与 daily 字段一致。

0. 文件与原则

| 文件 | 职责 |
|------|------|
| 新建 analysis/stock_pipeline.py | 单票深度分析唯一入口（Web + daily 共用） |
| web/pages/1_📈_个股分析.py | 只调 pipeline + 按区块展示；删重复「财务指标」 |
| main.py | _perform_mystery_analysis 内循环改为调 pipeline（或逐步替换） |

禁止：Web 再写一套与 main 不同的评分/主升浪逻辑。  
板块涨跌优先 板块指数 K 线（sector_kline / THS index-historical）；无数据再退化。

1. 新建 analysis/stock_pipeline.py

1.1 入口签名

#!/usr/bin/env python3
stock_pipeline.py - 单票深度分析统一入口（Web / daily 共用）
"""
输出可 JSON 序列化的 dict，键名与 Excel/报告对齐。
"""
from typing import Any, Dict, Optional
import pandas as pd


def analyze_one_stock(
    logic,                          # MysteryLogic 实例
    daily: pd.DataFrame,            # 已含指标的日K
    *,
    code: str = "",
    name: str = "",
    weekly: Optional[pd.DataFrame] = None,
    monthly: Optional[pd.DataFrame] = None,
    market_data: Optional[Dict] = None,
    industry_trend: Optional[bool] = None,
    industry_name: str = "未知",
    sector_kline: Optional[pd.DataFrame] = None,  # 板块指数日K（收盘价）
    financial: Optional[Dict] = None,
) -> Dict[str, Any]:
    """单票完整分析；禁止内部再拉行情（由调用方注入）。"""

1.2 板块 5/10/20 日涨跌

def _sector_returns(sector_kline: Optional[pd.DataFrame]) -> Dict[str, Any]:
    out = {
        '板块近5日涨跌': None,
        '板块近10日涨跌': None,
        '板块近20日涨跌': None,
        '板块趋势': None,  # True/False/None，供三振
    }
    if sector_kline is None or sector_kline.empty:
        return out
    df = sector_kline.copy()
    col = '收盘价' if '收盘价' in df.columns else (
        'close' if 'close' in df.columns else None)
    if not col or len(df)  0 视为走强（与 industry_trend 对齐时可覆盖）
    r10 = out['板块近10日涨跌']
    if r10 is not None:
        out['板块趋势'] = bool(r10 > 0)
    return out

调用方：有板块名时用 feeder/DB 取板块指数 K（load_sector_kline / fetch_block_daily），不要用成分股抽样。

1.3 多周期共振

复用 main._analyze_multi_period 逻辑（可抽成函数）：

def _multi_period(weekly, monthly) -> Dict[str, Any]:
    result = {'周线趋势': '未知', '月线趋势': '未知', '多周期共振': False}
与 main.py _analyze_multi_period 单票分支一致：
周/月 MA5>MA10>MA20 为向上；两者都向上 → 多周期共振=True
    ...
    return result

从 main.py 剪切单票判断到此处，main 循环改调本函数，避免两套规则。

1.4 组装结果（字段清单）

    sec = _sector_returns(sector_kline)
    ind_trend = industry_trend
    if ind_trend is None and sec.get('板块趋势') is not None:
        ind_trend = sec['板块趋势']

    signal = logic.comprehensive_signal_analysis(
        daily, weekly_data=weekly, market_data=market_data,
        industry_data=None, industry_trend=ind_trend)

    resonance = logic.three_resonance_analysis(
        daily, market_data=market_data,
        industry_trend=ind_trend, industry_data=None)

    bull = logic.main_bull_wave_analysis(daily)
    checklist = logic.main_bull_wave_checklist(daily, industry_trend=ind_trend)
    platform = logic.platform_breakthrough_analysis(
        daily, stock_code=code, weekly_data=weekly, monthly_data=monthly)
    technical = logic.technical_detail_capture(daily)
    multi = _multi_period(weekly, monthly)

判定依据1/2/3：取 bull['判定依据'] 去掉「判定结果:」行后的前 3 条
    reasons = [x for x in (bull.get('判定依据') or [])
               if not str(x).startswith('判定结果')]
    while len(reasons) 50继续走强",
    "主力资金连续流入", "行业板块同步走强",
]
if items:
    for it in items:
        st.markdown(f"- {it}")
else:
    for lb in LABELS:
        st.markdown(f"- {lb}: —")

（若 checklist 返回结构化 items: [{name, ok}]，优先用结构；否则保持详情字符串。）

2.5 平台突破分析

st.subheader("📦 平台突破分析")
p1, p2, p3, p4 = st.columns(4)
p1.metric("平台状态", report.get('平台状态', '未知'))
box = report.get('平台箱体')
p2.metric("平台箱体(近20日)", str(box) if box else "N/A")
p3.metric("突破信号", "是" if report.get('突破信号') else "否")
p4.metric("买横信号", "是" if report.get('买横信号') else "否")

2.6 财务：只保留一块

保留 「💰 财务数据」（PE/PB/股息率/ROE）。
删除**任何第二处「财务指标」subheader / 重复 metric（全文件搜索 财务指标、PE 展示块，只留一处）。

2.7 调用 pipeline

from analysis.stock_pipeline import analyze_one_stock

解析 industry_name、sector_kline（见下）
report = analyze_one_stock(
    logic, daily, code=code, name=name,
    weekly=weekly, monthly=monthly,  # monthly 可 resample
    market_data=market_data,
    industry_trend=industry_trend,
    industry_name=industry_name,
    sector_kline=sector_kline,
    financial=fi,  # 可先 ensure_financial 再传入
)
analysis_cache 写入 report 精简版或整包（注意 JSON）

板块 K 线获取（调用方）：

sector_kline = None
try:
优先 DB sector_kline / feeder 已有接口
    if hasattr(feeder, 'get_block_kline'):
        sector_kline = feeder.get_block_kline(industry_name)
    else:
        from ths_client import ThsOfficialClient
或 MysteryDB 读 sector_kline WHERE name/code
        ...
except Exception:
    sector_kline = None

有 pages_util 强度 map 时，industry_trend 仍可用得分>0；涨跌幅必须来自板块指数序列。

3. daily 与 Web 流程统一

3.1 目标流水线（两边相同）

日K(+指标) → 周/月K
→ 大盘 market_data
→ 所属板块名 + 板块指数K → 板块趋势 & 5/10/20涨跌
→ analyze_one_stock(...)
→ 写缓存 / 写报告行 / Web 展示

3.2 main.py 改造点

在 _perform_mystery_analysis 循环内：

原：分别调 three_resonance、main_bull_wave、comprehensive_signal、checklist、platform、technical、multi_period_map 再手工拼 dict。

改：

from analysis.stock_pipeline import analyze_one_stock

预先：market_data、industry 映射、sector_kline 按板块缓存 dict
sector_kl_cache = {}  # name -> DataFrame

for stock_code, indicators in indicators_data.items():
    ...
    ind_name = industry_info.get('行业', '未知')
    if ind_name not in sector_kl_cache:
        sector_kl_cache[ind_name] = load_sector_kline(ind_name)  # 实现见上
    report = analyze_one_stock(
        self.mystery_logic, indicators,
        code=stock_code, name=stock_name,
        weekly=weekly_df, monthly=monthly_df,
        market_data=market_data,
        industry_trend=industry_trend,
        industry_name=ind_name,
        sector_kline=sector_kl_cache.get(ind_name),
        financial=financial_data_map.get(stock_code),
    )
    analysis_results[stock_code] = report  # 报告生成器读同一键名

报告 Excel 列名与 report 键对齐：板块近5日涨跌、多周期共振、判定依据1…  

若旧报告读 板块近5日 等旧键，在 pipeline 末尾 双写别名 一周过渡：

out['板块近5日'] = out['板块近5日涨跌']
...

3.3 daily.sh / run_analysis.py

不改命令行；只要 main.analyze_stocks 走 pipeline，daily 任务自动对齐。

3.4 Web 缓存

db.set_analysis_cache(db_code, 'daily', last_date, {
    'report': report,  # 或剔除非序列化字段
    'signal': report.get('signal'),
})

命中缓存时直接 report = cached['report']，展示路径与冷路径相同。

4. 验收

| 项 | 标准 |
|----|------|
| Web 三振区 | 有板块趋势 + 近5/10/20 日涨跌（有板块K时非全 N/A） |
| 多周期 | 有「多周期共振」 |
| 主升浪 | 状态 + 依据1/2/3 + 满足数 + 综合判断 + 8 项列表 |
| 平台 | 状态、箱体、突破、买横 |
| 财务 | 仅「财务数据」一块 |
| 统一 | 同一 code、同一交易日，Web report 与 daily 行关键字段一致（评分/真三振/主升浪状态/平台状态） |

python -c "
from analysis.stock_pipeline import analyze_one_stock
print('import ok')
"

5. 提交说明

feat: 单票 analyze_one_stock 统一 Web/daily；展示板块涨跌与主升浪/平台全字段

6. 禁止

Web 继续只调 comprehensive_signal_analysis 却手写另一套主升浪判定  
板块 5/10/20 用随机成分股平均（与「指数优先」冲突时以指数为准）  
保留两处财务 metric 区块  
修改 8 项判定公式（仅展示与流水线统一，算法沿用 main_bull_wave_checklist）  

实施顺序：§1 pipeline → §2 Web 展示 → §3 main 循环替换 → 验收。

