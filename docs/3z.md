三振识别 —— 行业趋势判断优化 + 全部完整优化方案

一、行业趋势判断当前问题与优化目标

当前实现的问题：
仅用「价格 vs MA20 ±5%」简单统计强势/弱势行业数量
未考虑行业整体涨跌幅、成交额、持续性
对少数行业数据缺失或极端值敏感
无法区分「真板块启动」与「个别行业脉冲」

优化目标：
更稳健的行业强弱划分
引入行业整体涨幅与资金维度
增加持续性过滤（避免一日游）
支持行业数据缺失时的优雅降级
与大盘、个股、资金维度形成完整闭环

二、行业趋势优化后的核心逻辑

def analyze_industry_trend(self, industry_data: dict, lookback: int = 10) -> dict:
    """
    优化后的行业趋势判断
    industry_data: {行业名称: DataFrame}，DataFrame需含 收盘价、成交额（可选）
    """
    if not industry_data:
        return {
            "trend": "未知",
            "strength": 0,
            "strong_count": 0,
            "weak_count": 0,
            "detail": "无行业数据",
            "top_industries": []
        }

    strong = []
    weak = []
    neutral = []
    industry_scores = []

    for name, df in industry_data.items():
        if df is None or len(df)  0 else 0

2. 近N日涨幅（持续性）
        if len(df) >= lookback:
            change_n = (close / df['收盘价'].iloc[-lookback] - 1) * 100
        else:
            change_n = 0

3. 成交额变化（资金维度，可选）
        amount_score = 0
        if '成交额' in df.columns and len(df) >= 5:
            amount_ma = df['成交额'].iloc[-6:-1].mean()
            if amount_ma > 0:
                amount_ratio = latest['成交额'] / amount_ma
                if amount_ratio >= 1.5:
                    amount_score = 1

综合评分（-2 ~ +3）
        score = 0
        if bias > 5 and change_n > 3:
            score = 2 + amount_score          # 强势
        elif bias > 2 and change_n > 0:
            score = 1 + amount_score          # 偏强
        elif bias = 2:
            strong.append(name)
        elif score = weak_cnt + 2 and strong_cnt >= max(3, total * 0.25):
            trend = "向上"
            strength = min(100, int(strong_cnt / total * 100 + 20))
        elif weak_cnt >= strong_cnt + 2 and weak_cnt >= max(3, total * 0.25):
            trend = "向下"
            strength = min(100, int(weak_cnt / total * 100 + 20))
        else:
            trend = "震荡"
            strength = 30

记录最强行业（方便报告展示）
    top_industries = sorted(
        [x for x in industry_scores if x["score"] >= 2],
        key=lambda x: x["score"] + x["change_n"]/10,
        reverse=True
    )[:5]

    return {
        "trend": trend,
        "strength": strength,
        "strong_count": strong_cnt,
        "weak_count": weak_cnt,
        "neutral_count": len(neutral),
        "detail": f"强势{strong_cnt} / 弱势{weak_cnt} / 中性{len(neutral)}",
        "top_industries": [x["name"] for x in top_industries],
        "top_detail": top_industries
    }

三、全部完整优化方案（最终版）

1. 整体架构（四维共振）

| 维度 | 权重 | 核心判断 |
|------|------|----------|
| 个股趋势 | 30 | 均线多头 + 基础过滤 + 股价 > MA20 |
| 大盘趋势 | 25 | 收盘 > MA20 且 > MA60 + 位置评估 |
| 行业趋势 | 25 | 优化后的强势行业数量 + 涨幅 + 资金 |
| 资金确认 | 20 | 量比 ≥1.5 或成交额显著放大（新增） |

真三振条件：四维全部向好 + 资金活跃 + 大盘非高位
三级（真三振）阈值：≥85 分
高位自动降权 15 分

2. 完整评分函数（最终版）

def calculate_resonance_score(self,
                              individual_result: dict,
                              market_result: dict,
                              industry_result: dict,
                              capital_result: dict = None) -> dict:
    score = 0
    details = []

1. 个股（30）
    stock_ok = individual_result.get('基础过滤', False) and individual_result.get('均线多头', False)
    if stock_ok:
        score += 30
        details.append("个股趋势✓(+30)")
    else:
        details.append("个股趋势✗")

2. 大盘（25）
    if market_result.get('trend') == "向上":
        score += 25
        details.append("大盘向上✓(+25)")
    else:
        details.append(f"大盘{market_result.get('trend', '未知')}")

3. 行业（25）——使用优化后逻辑
    if industry_result.get('trend') == "向上":
        score += 25
        details.append(f"行业向上✓(+25) {industry_result.get('detail', '')}")
    else:
        details.append(f"行业{industry_result.get('trend', '未知')}")

4. 资金（20）
    capital_score = 0
    capital_active = False
    if capital_result:
        capital_score = capital_result.get('score', 0)
        capital_active = capital_result.get('active', False)
        score += capital_score
        details.append(f"资金确认(+{capital_score}) {capital_result.get('detail', '')}")

高位惩罚
    if market_result.get('position') == "高位":
        score = max(0, score - 15)
        details.append("大盘高位惩罚(-15)")

最终定级
    is_true = (score >= 85 and capital_active and 
               market_result.get('trend') == "向上" and 
               industry_result.get('trend') == "向上" and stock_ok)

    if is_true:
        level = "真三振（三级）"
        advice = "强烈建议关注！可能是大级别行情启动窗口，大资金跨层级共振"
    elif score >= 70:
        level = "二级共振"
        advice = "可关注，需持续观察资金与板块持续性"
    elif score >= 45:
        level = "一级共振"
        advice = "观望为主，等待更明确的资金与板块信号"
    else:
        level = "无共振"
        advice = "建议观望，留住本金，等待真正的三振机会"

    return {
        "score": round(score, 1),
        "level": level,
        "advice": advice,
        "is_true_three_strike": is_true,
        "details": details,
        "capital_active": capital_active,
        "industry_top": industry_result.get('top_industries', [])
    }

3. 需要同步修改的调用处（mystery_logic.py）

在 three_resonance_analysis 中：

行业分析改用新方法
industry_result = self.resonance_analyzer.analyze_industry_trend(industry_data_dict)

资金分析
capital_result = self.resonance_analyzer.analyze_capital_flow(data)

最终评分
resonance = self.resonance_analyzer.calculate_resonance_score(
    individual_result=individual_result,
    market_result=market_result,
    industry_result=industry_result,
    capital_result=capital_result
)

四、优化效果预期

| 项目 | 优化前 | 优化后 |
|------|--------|--------|
| 真三振识别精度 | 中等（易受假突破影响） | 显著提高（必须资金+行业同步） |
| 高位假信号 | 较多 | 大幅减少（位置惩罚） |
| 行业判断稳定性 | 低（易受单日波动） | 高（涨幅+资金+数量双重过滤） |
| 报告可解释性 | 一般 | 强（可展示最强板块列表） |
| 与原帖匹配度 | 70% | 90%+ |

五、落地步骤建议

替换 resonance_analyzer.py 中的三个核心方法（analyze_market_trend、analyze_industry_trend、analyze_capital_flow + calculate_resonance_score）
更新 mystery_logic.py 的调用
确保行业数据字典能正常传入（从通达信板块或申万行业获取）
回测 2024-2025 年几轮主升浪启动前后的命中情况
在报告中增加「最强板块」展示

需要我现在输出完整可直接替换的 resonance_analyzer.py 文件（包含所有辅助方法和完整类结构）吗？




-- coding: utf-8 --
"""
resonance_analyzer.py
三振共振分析模块（完整优化版）

定义来源：《Mistery趋势交易论》 + @Mimiwftt 原帖
三振 = 大盘 + 板块（行业） + 个股 集体上涨，且有大资金涌入

优化点：
增加资金活跃度验证（量比、成交额、换手率）
大盘位置评估（高位惩罚）
行业趋势更稳健（涨幅 + 相对MA20 + 资金）
真三振与普通共振严格区分
"""

from future import annotations
import logging
from typing import Dict, List, Optional, Any
import pandas as pd
import numpy as np

logger = logging.getLogger(name)


class ResonanceAnalyzer:
    """三振共振分析器"""

    def init(self):
        pass

------------------------------------------------------------------
1. 大盘趋势分析（含位置评估）
------------------------------------------------------------------
    def analyze_market_trend(self, index_data: Optional[pd.DataFrame]) -> Dict[str, Any]:
        """
        大盘趋势判断
        返回: trend(向上/向下/震荡/未知), strength, position(高位/中位/低位), detail
        """
        if index_data is None or len(index_data)  ma20 and close > ma60:
            trend = "向上"
        elif close = 20:
            past = float(df[close_col].iloc[-20])
            if past > 0:
                change = (close / past - 1) * 100
                strength = min(abs(change), 100.0)

位置评估（近120日）
        position = "未知"
        if len(df) >= 120:
            high_120 = float(df[close_col].iloc[-120:].max())
            low_120 = float(df[close_col].iloc[-120:].min())
            if high_120 > low_120:
                pos_pct = (close - low_120) / (high_120 - low_120)
                if pos_pct >= 0.85:
                    position = "高位"
                elif pos_pct  Dict[str, Any]:
        """
        优化后的行业趋势判断
        industry_data: {行业名: DataFrame}
        """
        empty_result = {
            "trend": "未知",
            "strength": 0,
            "strong_count": 0,
            "weak_count": 0,
            "neutral_count": 0,
            "detail": "无行业数据",
            "top_industries": [],
            "top_detail": []
        }

        if not industry_data:
            return empty_result

        strong, weak, neutral = [], [], []
        industry_scores = []

        for name, df in industry_data.items():
            if df is None or len(df)  0 else 0.0

近N日涨幅
            change_n = 0.0
            if len(df) >= lookback:
                past = float(df[close_col].iloc[-lookback])
                if past > 0:
                    change_n = (close / past - 1) * 100

成交额放大（可选）
            amount_score = 0
            if "成交额" in df.columns and len(df) >= 6:
                amount_ma = df["成交额"].iloc[-6:-1].mean()
                if amount_ma > 0:
                    amount_ratio = float(latest["成交额"]) / amount_ma
                    if amount_ratio >= 1.5:
                        amount_score = 1

综合得分
            score = 0
            if bias > 5 and change_n > 3:
                score = 2 + amount_score
            elif bias > 2 and change_n > 0:
                score = 1 + amount_score
            elif bias = 2:
                strong.append(name)
            elif score = weak_cnt + 2 and strong_cnt >= max(3, int(total * 0.25)):
            trend = "向上"
            strength = min(100, int(strong_cnt / total * 100) + 20)
        elif weak_cnt >= strong_cnt + 2 and weak_cnt >= max(3, int(total * 0.25)):
            trend = "向下"
            strength = min(100, int(weak_cnt / total * 100) + 20)
        else:
            trend = "震荡"
            strength = 30

最强行业列表
        top_detail = sorted(
            [x for x in industry_scores if x["score"] >= 2],
            key=lambda x: (x["score"], x["change_n"]),
            reverse=True
        )[:5]
        top_industries = [x["name"] for x in top_detail]

        return {
            "trend": trend,
            "strength": strength,
            "strong_count": strong_cnt,
            "weak_count": weak_cnt,
            "neutral_count": neutral_cnt,
            "detail": f"强势{strong_cnt} / 弱势{weak_cnt} / 中性{neutral_cnt}",
            "top_industries": top_industries,
            "top_detail": top_detail
        }

------------------------------------------------------------------
3. 资金活跃度分析（新增核心）
------------------------------------------------------------------
    def analyze_capital_flow(self, stock_data: Optional[pd.DataFrame]) -> Dict[str, Any]:
        """
        资金活跃度判断（区分真三振的关键）
        """
        if stock_data is None or len(stock_data) = 6 else df[vol_col].mean()
        volume_ratio = float(latest[vol_col]) / (vol_ma5 + 1e-8)

        score = 0
        reasons = []

量比
        if volume_ratio >= 1.8:
            score += 12
            reasons.append(f"量比{volume_ratio:.1f}")
        elif volume_ratio >= 1.5:
            score += 8
            reasons.append(f"量比{volume_ratio:.1f}")

成交额
        if amount_col in df.columns and len(df) >= 6:
            amount_ma5 = df[amount_col].iloc[-6:-1].mean()
            if amount_ma5 > 0:
                amount_ratio = float(latest[amount_col]) / amount_ma5
                if amount_ratio >= 1.6:
                    score += 5
                    reasons.append("成交额放大")

换手率
        if turnover_col and turnover_col in df.columns:
            turnover = float(latest[turnover_col])
            if turnover >= 3.0:
                score += 3
                reasons.append(f"换手{turnover:.1f}%")

        score = min(score, 20)
        active = score >= 8 or volume_ratio >= 1.5

        return {
            "active": active,
            "score": score,
            "volume_ratio": round(volume_ratio, 2),
            "detail": " | ".join(reasons) if reasons else "资金平淡"
        }

------------------------------------------------------------------
4. 三振共振综合评分（最终版）
------------------------------------------------------------------
    def calculate_resonance_score(
        self,
        individual_result: Dict[str, Any],
        market_result: Dict[str, Any],
        industry_result: Dict[str, Any],
        capital_result: Optional[Dict[str, Any]] = None
    ) -> Dict[str, Any]:
        """
        四维共振评分
        个股30 + 大盘25 + 行业25 + 资金20 = 100
        """
        score = 0.0
        details: List[str] = []

1. 个股趋势（30分）
        stock_ok = bool(individual_result.get("基础过滤", False)) and \
                   bool(individual_result.get("均线多头", False))
        if stock_ok:
            score += 30
            details.append("个股趋势✓(+30)")
        else:
            details.append("个股趋势✗")

2. 大盘趋势（25分）
        market_trend = market_result.get("trend", "未知")
        if market_trend == "向上":
            score += 25
            details.append("大盘向上✓(+25)")
        else:
            details.append(f"大盘{market_trend}")

3. 行业趋势（25分）
        industry_trend = industry_result.get("trend", "未知")
        if industry_trend == "向上":
            score += 25
            details.append(f"行业向上✓(+25) [{industry_result.get('detail', '')}]")
        else:
            details.append(f"行业{industry_trend}")

4. 资金确认（20分）
        capital_score = 0
        capital_active = False
        if capital_result:
            capital_score = capital_result.get("score", 0)
            capital_active = capital_result.get("active", False)
            score += capital_score
            details.append(f"资金(+{capital_score}) {capital_result.get('detail', '')}")

高位惩罚
        if market_result.get("position") == "高位":
            score = max(0.0, score - 15)
            details.append("大盘高位惩罚(-15)")

最终定级
        is_true = (
            score >= 85
            and capital_active
            and market_trend == "向上"
            and industry_trend == "向上"
            and stock_ok
        )

        if is_true:
            level = "真三振（三级）"
            advice = "强烈建议关注！可能是大级别行情启动窗口，大资金跨层级共振"
        elif score >= 70:
            level = "二级共振"
            advice = "可关注，需持续观察资金与板块持续性"
        elif score >= 45:
            level = "一级共振"
            advice = "观望为主，等待更明确的资金与板块信号"
        else:
            level = "无共振"
            advice = "建议观望，留住本金，等待真正的三振机会"

        return {
            "score": round(score, 1),
            "level": level,
            "advice": advice,
            "is_true_three_strike": is_true,
            "details": details,
            "capital_active": capital_active,
            "industry_top": industry_result.get("top_industries", []),
            "market_position": market_result.get("position", "未知")
        }

------------------------------------------------------------------
5. 便捷入口（供 mystery_logic 调用）
------------------------------------------------------------------
    def analyze(
        self,
        stock_data: pd.DataFrame,
        individual_result: Dict[str, Any],
        market_data: Optional[pd.DataFrame] = None,
        industry_data: Optional[Dict[str, pd.DataFrame]] = None
    ) -> Dict[str, Any]:
        """
        一站式三振分析入口
        """
        market_result = self.analyze_market_trend(market_data)
        industry_result = self.analyze_industry_trend(industry_data)
        capital_result = self.analyze_capital_flow(stock_data)

        resonance = self.calculate_resonance_score(
            individual_result=individual_result,
            market_result=market_result,
            industry_result=industry_result,
            capital_result=capital_result
        )

兼容旧字段
        resonance["三级共振"] = resonance["is_true_three_strike"]
        resonance["共振评分"] = resonance["score"]
        resonance["共振级别"] = resonance["level"]
        resonance["建议"] = resonance["advice"]

        return resonance

测试入口（可选）
if name == "main":
    print("ResonanceAnalyzer 模块加载成功（完整优化版）")

使用说明：
直接用此文件覆盖原 analysis/resonance_analyzer.py。
在 mystery_logic.py 中调用示例：

analyzer = ResonanceAnalyzer()
result = analyzer.analyze(
    stock_data=df,
    individual_result={"基础过滤": True, "均线多头": True},
    market_data=index_df,
    industry_data=industry_dict   # {行业名: DataFrame}
)

此版本已完整包含此前讨论的所有优化（资金、位置、行业稳健性、真三振严格判定）。








